/**
 * Entrada de compatibilidade para imagens e supervisores ainda configurados
 * com dist/src/server.js. Novas execuções devem apontar para main.ts.
 */
import { bootstrap } from './main';

void bootstrap();
